<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.bootjava.palette.cart.mapper.CartMapper">
	<select id="cartSelectList" resultType="co.bootjava.palette.cart.CartVO">
		SELECT * FROM CART
	</select>
	
	<select id="cartAccountSelectList" resultType="co.bootjava.palette.cart.CartVO">
		SELECT * FROM CART WHERE ACCOUNT = #{account}
	</select>
	
	<select id="cartSelect" resultType="co.bootjava.palette.cart.CartVO">
		SELECT * FROM CART WHERE PRODUCT_NUMBER = #{productNumber} AND ACCOUNT = #{account}
	</select>
	
	<insert id="cartInsert" >
		INSERT INTO CART (CART_NUMBER, PRODUCT_NUMBER, PRODUCT_COUNT, PRODUCT_PRICE, ACCOUNT)
		 VALUES(TEST_SEQ.NEXTVAL,#{productNumber},1,#{productPrice},#{account})
	</insert>
	
	<delete id="cartDelete">
	</delete>
	
	<update id="cartUpdatePlus">
		UPDATE CART SET PRODUCT_COUNT=PRODUCT_COUNT+1
		 WHERE CART_NUMBER=#{cartNumber}
	</update>
	
	<update id="cartUpdateMinus">
		UPDATE CART SET PRODUCT_COUNT=PRODUCT_COUNT-1
		 WHERE CART_NUMBER=#{cartNumber}
	</update>
</mapper>